{
  "name": "@ai-hackathon/mcp-server",
  "version": "0.0.1",
  "description": "A simple MCP server in TypeScript",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "ts-node src/server.ts",
    "start": "node dist/server.js",
    "server:inspect:dev": "npx @modelcontextprotocol/inspector ts-node src/server.ts",
    "server:inspect:prod": "npm run build && npx @modelcontextprotocol/inspector npm run start"
  },
  "keywords": [
    "mcp",
    "server",
    "typescript"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}